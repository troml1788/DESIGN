# 协作原则
#协作原则
+ 第一个标签（所属大类）
`#机械`------ #机械
`#电气`------ #电气
+ 第二个标签（具体技术）
`#传感器` #传感器
+ 剩余标签随意添加
......


# 使用说明
如果遇到不一样的情况
+ 远程和本地不一样
```shell
# 1. 获取远程所有最新提交信息（只是拉元数据，不会改本地文件）
git fetch --all

# 2. 把当前本地分支指针，硬重置到远程分支。把本地文件完全改成远程版本
# 把 main 换成你的分支名，比如 master / dev
git reset --hard origin/main
```
